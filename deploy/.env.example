# Copy to /opt/fxlab/.env ON THE SERVER (chmod 600). Never commit the real file.
IMAGE=ghcr.io/azayst/fx
DB_PASSWORD=change-me-long-random
API_KEY=change-me-long-random
# Loopback port nginx proxies to. Change it (and nginx proxy_pass) if 8100 is taken.
APP_PORT=8100
